# web-irc-client
Web-based IRC client

Requires npm
 
To build:
```
npm install
npm run build
```

To enable SSL place origin certificates in:
```
<docroot>/env/key.pem
<docroot>/env/cert.pem
```

To run:
```
npm run start
```
